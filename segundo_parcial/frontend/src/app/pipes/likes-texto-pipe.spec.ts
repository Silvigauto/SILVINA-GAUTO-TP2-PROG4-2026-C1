import { LikesTextoPipe } from './likes-texto-pipe';

describe('LikesTextoPipe', () => {
  it('create an instance', () => {
    const pipe = new LikesTextoPipe();
    expect(pipe).toBeTruthy();
  });
});
