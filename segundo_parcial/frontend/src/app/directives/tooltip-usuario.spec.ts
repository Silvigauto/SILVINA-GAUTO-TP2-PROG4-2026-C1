import { TooltipUsuario } from './tooltip-usuario';

describe('TooltipUsuario', () => {
  it('should create an instance', () => {
    const directive = new TooltipUsuario();
    expect(directive).toBeTruthy();
  });
});
