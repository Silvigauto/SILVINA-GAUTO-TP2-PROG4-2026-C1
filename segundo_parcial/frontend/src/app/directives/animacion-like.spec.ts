import { AnimacionLike } from './animacion-like';

describe('AnimacionLike', () => {
  it('should create an instance', () => {
    const directive = new AnimacionLike();
    expect(directive).toBeTruthy();
  });
});
