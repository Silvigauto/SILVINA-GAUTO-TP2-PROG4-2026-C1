import { ConfirmarAccion } from './confirmar-accion';

describe('ConfirmarAccion', () => {
  it('should create an instance', () => {
    const directive = new ConfirmarAccion();
    expect(directive).toBeTruthy();
  });
});
